enum CLIENT_EVENT {
  SEBD_COMPONENT = "SEBD_COMPONENT",
}

enum SERVER_EVENT {
  RETURN_COMPONENT = "RETURN_COMPONENT",
}

const COMPONENT = {
  CLIENT_EVENT,
  SERVER_EVENT,
};

export default COMPONENT;
